# WordPress CMS URL for headless content
# Set this to your WordPress installation URL (no trailing slash)
# If empty, the site falls back to hardcoded content in src/lib/fallback-data.ts
PUBLIC_WP_URL=
